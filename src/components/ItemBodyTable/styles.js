import styled from 'styled-components';

export const ItemBodyComponent = styled.td`
    padding: 1rem;
    font-weight: 300;
    border-top: 1px solid var(--color-line-in-white);
    max-width: 25rem;
`;